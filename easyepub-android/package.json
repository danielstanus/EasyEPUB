{
  "name": "easyepub-android",
  "version": "0.1.0",
  "private": true,
  "description": "EasyEPUB Android app (Capacitor wrapper around the EasyEPUB web reader)",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:android": "cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.3",
    "@capacitor/browser": "^6.0.6",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0"
  }
}
